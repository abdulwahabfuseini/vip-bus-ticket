import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "VIP Bus Ticking | Add Bus",
  description: "Add Bus Details",
};

const AddBus = () => {
  return <div>AddBus</div>;
};

export default AddBus;
 