import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "VIP Online Bus Ticketng | Supports",
  description: "Need Help? Contact Our Support Team",
};

const Supports = () => {
  return <div>Supports</div>;
};

export default Supports;
